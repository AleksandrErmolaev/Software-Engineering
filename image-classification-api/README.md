# Image Classification API

Простое и эффективное REST API для классификации изображений с использованием предобученной модели ResNet50. Позволяет определять объекты на изображениях через файлы или URL.

## 🚀 Возможности

- 📷 Классификация изображений из файлов
- 🌐 Классификация изображений по URL
- 🔮 Топ-N предсказаний с вероятностями
- 📊 Автоматическая документация API
- 🏥 Health-check эндпоинты
- 🔒 Поддержка CORS

## 🛠 Технологии

- **FastAPI** - современный, быстрый веб-фреймворк
- **TensorFlow/Keras** - машинное обучение
- **ResNet50** - предобученная модель классификации
- **Pydantic** - валидация данных
- **Pillow** - обработка изображений
- **Uvicorn** - ASGI сервер


## 🚀 Запуск

### Разработка (с авто-перезагрузкой)

```bash
python run.py
```

Или напрямую через uvicorn:

```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```


## 📚 Использование API

После запуска API будет доступно по адресу: `http://localhost:8000`

### Автоматическая документация

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### Эндпоинты

#### 1. Проверка здоровья API

```http
GET /health
```

**Ответ:**
```json
{
  "status": "healthy",
  "model_loaded": true,
  "model_name": "ResNet50"
}
```

#### 2. Классификация изображения из файла

```http
POST /predict
Content-Type: multipart/form-data
```

**Параметры:**
- `file`: файл изображения (обязательно)
- `top_k`: количество возвращаемых предсказаний (по умолчанию: 5)

**Пример запроса (curl):**
```bash
curl -X POST "http://localhost:8000/predict?top_k=3" \
  -H "accept: application/json" \
  -H "Content-Type: multipart/form-data" \
  -F "file=@your_image.jpg"
```

#### 3. Классификация изображения по URL

```http
POST /predict/url?url=IMAGE_URL&top_k=5
```

**Параметры:**
- `url`: URL изображения (обязательно)
- `top_k`: количество возвращаемых предсказаний (по умолчанию: 5)

**Пример запроса (curl):**
```bash
curl -X POST "http://localhost:8000/predict/url?url=https://example.com/image.jpg&top_k=3"
```

## 📋 Примеры ответов

### Успешный ответ

```json
{
  "status": "success",
  "predictions": [
    {
      "rank": 1,
      "class_id": "n02123159",
      "class_name": "tiger_cat",
      "confidence": 0.456789,
      "confidence_percentage": "45.68%"
    },
    {
      "rank": 2,
      "class_id": "n02123045",
      "class_name": "tabby",
      "confidence": 0.234567,
      "confidence_percentage": "23.46%"
    }
  ],
  "top_prediction": {
    "rank": 1,
    "class_id": "n02123159",
    "class_name": "tiger_cat",
    "confidence": 0.456789,
    "confidence_percentage": "45.68%"
  }
}
```

### Ответ с ошибкой

```json
{
  "status": "error",
  "message": "Ошибка загрузки изображения из URL"
}
```

## 🧪 Тестирование

### Запуск тестов

```bash
python tests/test_api.py
```

### Ручное тестирование

1. **Проверка здоровья:**
```bash
curl http://localhost:8000/health
```

2. **Тест с файлом:**
```bash
curl -X POST "http://localhost:8000/predict?top_k=3" \
  -F "file=@static/images/test_image.jpg"
```

3. **Тест с URL:**
```bash
curl -X POST "http://localhost:8000/predict/url?url=https://github.com/opencv/opencv/raw/master/samples/data/basketball1.png&top_k=3"
```

## 📁 Структура проекта

```
image-classification-api/
├── app/
│   ├── __init__.py
│   ├── main.py              # Основное приложение FastAPI
│   ├── classifier.py        # Модель классификатора
│   └── schemas.py           # Pydantic схемы
├── tests/
│   └── test_api.py          # Тесты API
├── static/
│   └── images/              # Тестовые изображения
├── requirements.txt         # Зависимости
├── run.py                  # Скрипт запуска
└── README.md              # Документация
```


### Конфигурация модели

Модель автоматически загружает предобученные веса ImageNet при инициализации. Для использования кастомной модели замените путь в `classifier.py`.
