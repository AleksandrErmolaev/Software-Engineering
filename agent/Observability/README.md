# 🌤️ Weather AI Agent

Агент для получения погоды с LLM-анализом и полной observability системой.

## 📁 Структура
```
weather-ai-agent/
├── agent_v2.py          # Основной код
├── requirements.txt     # Зависимости
├── logs/               # Логи (авто)
├── traces/             # Трейсы (авто)
└── README.md
```

## 🚀 Быстрый старт

1. Установите зависимости:

```bash
pip install -r requirements.txt
```

2. Убедитесь, что Ollama запущен:

```bash
ollama serve
```

3. Запустите агента:

```bash
python agent_v2.py
```

## 🔧 Настройка

По умолчанию используются:

- OpenWeatherMap API ключ: предоставленный в коде
- LLM: Ollama с моделью llama3.1:8b
- Порт метрик: 8000

Для смены конфигурации отредактируйте переменные в agent_v2.py

## 📊 Функции

Погода: Текущая и прогноз (до 120 часов) через OpenWeatherMap
LLM-анализ: Детальные отчеты и рекомендации (Ollama)
Observability:

- Метрики Prometheus: http://localhost:8000/metrics
- Логи: logs/weather_agent_*.log
- Трейсы OpenTelemetry: traces/trace_*.json

Интерфейс: Консольный дашборд с статистикой

## 🛠️ Использование

1. Введите город
2. Укажите 0 для текущей погоды или часы для прогноза
3. Получите развернутый отчет с рекомендациями
